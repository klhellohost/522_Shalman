<Page x:Class="_522_Shalman.Pages.AddCategoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:_522_Shalman.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddCategoryPage">

	<Grid Margin="35,40">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Заголовок -->
		<TextBlock Text="Название категории:"
                   FontSize="19"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"
                   Style="{StaticResource LinkTextBlockStyle}"/>

		<!-- TextBox — без нижнего отступа -->
		<TextBox x:Name="TBCategoryName"
                 Grid.Row="0"
                 Margin="0,35,0,0"
                 Height="38"
                 Padding="12,0"
                 FontSize="15"
                 Style="{StaticResource TextBoxStyle}"
                 Text="{Binding Name}"/>

		<!-- Кнопки — слева, с увеличенным отступом сверху -->
		<StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    Margin="0,32,0,0">
			<Button x:Name="ButtonClean"
                    Content="Очистить"
                    Width="115"
                    Height="36"
                    Margin="0,0,14,0"
                    Padding="0"
                    Style="{StaticResource ButtonStyle}"
                    Click="ButtonClean_Click"/>
			<Button x:Name="ButtonSaveCategory"
                    Content="Сохранить"
                    Width="115"
                    Height="36"
                    Margin="0"
                    Padding="0"
                    Style="{StaticResource ButtonStyle}"
                    Click="ButtonSaveCategory_Click"/>
		</StackPanel>
	</Grid>
</Page>